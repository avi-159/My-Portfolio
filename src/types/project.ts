// src/types/project.ts
/*export type Project = {
  slug: string; // used in URL, e.g. /projects/ab-test
  title: string;
  summary: string;
  tags: string[];
  year: number;
  cover?: string; // /projects/ab-test/cover.jpg (optional)
  metrics?: { label: string; value: string }[];
};*/
